# Reference this view as omni_dbt__int_last30au
schema_label: ""

schema: omni_dbt
table_name: INT_LAST30AU

dimensions:
  date:
    sql: '"DATE"'

  organization_uid:
    sql: '"ORGANIZATION_UID"'

  location:
    sql: '"LOCATION"'

  platform:
    sql: '"PLATFORM"'

  l30au:
    sql: '"L30AU"'

measures:
  count:
    aggregate_type: count
