# Reference this view as omni_dbt_staging__stg_salesforce__campaign_member_information
schema_label: ""
#This description was pulled from dbt.
description: Staging model for Salesforce Campaign Member object. Cleans and
  standardizes raw campaign member data for use in marketing and engagement
  analytics.

schema: omni_dbt_staging
table_name: STG_SALESFORCE__CAMPAIGN_MEMBER_INFORMATION

dimensions:
  id:
    sql: '"ID"'
    format: ID
    #This description was pulled from dbt.
    description: Unique Salesforce Campaign Member ID. Serves as the primary key for
      the record.
    primary_key: true

  name:
    sql: '"NAME"'

  title:
    sql: '"TITLE"'

  email:
    sql: '"EMAIL"'

  street:
    sql: '"STREET"'

  city:
    sql: '"CITY"'

  state:
    sql: '"STATE"'

  postal_code:
    sql: '"POSTAL_CODE"'

  country:
    sql: '"COUNTRY"'

  campaign_member_geo_c:
    sql: '"CAMPAIGN_MEMBER_GEO_C"'

  _fivetran_synced:
    sql: '"_FIVETRAN_SYNCED"'
    hidden: true

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_salesforce__campaign_member_information
  target_schema: STAGING
  description: Staging model for Salesforce Campaign Member object. Cleans and
    standardizes raw campaign member data for use in marketing and engagement
    analytics.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (
        select * from {{ source('campaign_member', 'campaign_member') }} where is_deleted != 'true'
    )


    select
        id,
       NAME,
       TITLE,
       EMAIL,
       STREET,
       CITY,
       STATE,
       POSTAL_CODE,
       COUNTRY,
       CAMPAIGN_MEMBER_GEO_C,
        _fivetran_synced
    from source
