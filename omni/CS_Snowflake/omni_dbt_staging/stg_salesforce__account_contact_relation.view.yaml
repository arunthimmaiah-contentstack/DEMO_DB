# Reference this view as omni_dbt_staging__stg_salesforce__account_contact_relation
schema_label: ""
#This description was pulled from dbt.
description: Staging model for Salesforce Account-Contact Relationship object.
  Standardizes and filters relationship data between Accounts and Contacts for
  downstream use in reporting and analytics.

schema: omni_dbt_staging
table_name: STG_SALESFORCE__ACCOUNT_CONTACT_RELATION

dimensions:
  account_contact_relation_id:
    sql: '"ACCOUNT_CONTACT_RELATION_ID"'
    format: ID
    #This description was pulled from dbt.
    description: Unique identifier for the Account-Contact relationship record in
      Salesforce.

  account_id:
    sql: '"ACCOUNT_ID"'
    format: ID

  contact_id:
    sql: '"CONTACT_ID"'
    format: ID

  is_active:
    sql: '"IS_ACTIVE"'
    #This description was pulled from dbt.
    description: Boolean flag indicating whether the Account-Contact relationship is
      currently active.
    all_values: [ "TRUE", "FALSE", "true", "false" ]

  is_deleted:
    sql: '"IS_DELETED"'
    #This description was pulled from dbt.
    description: Flag indicating whether the record has been deleted in Salesforce.
    all_values: [ "TRUE", "FALSE", "true", "false" ]

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_salesforce__account_contact_relation
  target_schema: STAGING
  description: Staging model for Salesforce Account-Contact Relationship object.
    Standardizes and filters relationship data between Accounts and Contacts for
    downstream use in reporting and analytics.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (
        select * from {{ source('account_contact_relation', 'account_contact_relation') }}
    )

    select
        id as account_contact_relation_id, 
        account_id,
        contact_id,
        is_active,
        is_deleted
    from source
