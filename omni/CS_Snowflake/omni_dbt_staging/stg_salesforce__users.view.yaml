# Reference this view as omni_dbt_staging__stg_salesforce__users
schema_label: ""
#This description was pulled from dbt.
description: Staging model for Salesforce User object. Cleans, standardizes, and
  prepares user data for downstream analytics and reporting.

schema: omni_dbt_staging
table_name: STG_SALESFORCE__USERS

dimensions:
  user_id:
    sql: '"USER_ID"'
    format: ID
    #This description was pulled from dbt.
    description: Unique Salesforce User ID.

  user_name:
    sql: '"USER_NAME"'
    #This description was pulled from dbt.
    description: Full name of the user.

  company_name:
    sql: '"COMPANY_NAME"'
    #This description was pulled from dbt.
    description: Company or organization name associated with the user.

  email:
    sql: '"EMAIL"'
    #This description was pulled from dbt.
    description: Email address of the user.

  phone:
    sql: '"PHONE"'
    #This description was pulled from dbt.
    description: Phone number of the user.

  is_active:
    sql: '"IS_ACTIVE"'
    #This description was pulled from dbt.
    description: Indicates whether the user is active in Salesforce (TRUE/FALSE).
    all_values: [ "TRUE", "FALSE", "true", "false" ]

  time_zone_sid_key:
    sql: '"TIME_ZONE_SID_KEY"'
    #This description was pulled from dbt.
    description: Time zone of the user.

  user_role_id:
    sql: '"USER_ROLE_ID"'
    format: ID
    #This description was pulled from dbt.
    description: Salesforce role ID assigned to the user.

  region:
    sql: '"REGION"'
    #This description was pulled from dbt.
    description: Region of the user.

  sub_region:
    sql: '"SUB_REGION"'
    #This description was pulled from dbt.
    description: Sub-region of the user.

  created_date:
    sql: '"CREATED_DATE"'
    #This description was pulled from dbt.
    description: Date when the user was created in Salesforce.

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_salesforce__users
  target_schema: STAGING
  description: Staging model for Salesforce User object. Cleans, standardizes, and
    prepares user data for downstream analytics and reporting.
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (
        select * from {{ source('salesforce_users', 'user') }} 
    )

    select
        id as user_id,
        name as user_name,
        company_name,
        email,
        phone,
        is_active,
        time_zone_sid_key,
        user_role_id,
        region_c as region,
        sub_region_c as sub_region,
        created_date
    from source
  referenced_by: [ int_salesforce__user_unified ]
