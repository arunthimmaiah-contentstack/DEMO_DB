# Reference this view as omni_dbt_staging__stg_daily_stack_db_usage
schema_label: ""

schema: omni_dbt_staging
table_name: STG_DAILY_STACK_DB_USAGE

dimensions:
  source_file_name:
    sql: '"SOURCE_FILE_NAME"'

  file_row_number:
    sql: '"FILE_ROW_NUMBER"'

  batch_number:
    sql: '"BATCH_NUMBER"'

  data_sync_date:
    sql: '"DATA_SYNC_DATE"'

  org_uid:
    sql: '"ORG_UID"'

  org_name:
    sql: '"ORG_NAME"'

  stack_name:
    sql: '"STACK_NAME"'

  owner_email:
    sql: '"OWNER_EMAIL"'

  stack_api_key:
    sql: '"STACK_API_KEY"'

  content_types:
    sql: '"CONTENT_TYPES"'

  entries:
    sql: '"ENTRIES"'

  assets:
    sql: '"ASSETS"'

  environments:
    sql: '"ENVIRONMENTS"'

  locales:
    sql: '"LOCALES"'

  roles:
    sql: '"ROLES"'

  webhooks:
    sql: '"WEBHOOKS"'

  extensions:
    sql: '"EXTENSIONS"'

  saved_searches:
    sql: '"SAVED_SEARCHES"'

  custom_roles:
    sql: '"CUSTOM_ROLES"'

  global_fields:
    sql: '"GLOBAL_FIELDS"'

  date:
    sql: '"DATE"'

  terms:
    sql: '"TERMS"'

  branches:
    sql: '"BRANCHES"'

  taxonomies:
    sql: '"TAXONOMIES"'

  platform:
    sql: '"PLATFORM"'

  location:
    sql: '"LOCATION"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_daily_stack_db_usage
  target_schema: STAGING
  config:
    schema: staging
    materialized: view
  code: |-
    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        TERMS,
        BRANCHES,
        TAXONOMIES,
    	'AWS' as PLATFORM,
    	'NA' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('aws_stack_db_usage_na','stack_db_usage_na_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('aws_stack_db_usage_na','stack_db_usage_na_f')}}) 
    )
    where rn = 1

    union all

    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        TERMS,
        BRANCHES,
        TAXONOMIES,
    	'AWS' as PLATFORM,
    	'EU' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('aws_stack_db_usage_eu','stack_db_usage_eu_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('aws_stack_db_usage_eu','stack_db_usage_eu_f')}}) 
    )
    where rn = 1

    union all

    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        TERMS,
        BRANCHES,
        TAXONOMIES,
    	'AWS' as PLATFORM,
    	'AU' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('aws_stack_db_usage_au','stack_db_usage_au_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('aws_stack_db_usage_au','stack_db_usage_au_f')}}) 
    )
    where rn = 1

    union all

    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        TERMS,
        BRANCHES,
        TAXONOMIES,
    	'AZ' as PLATFORM,
    	'NA' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('az_stack_db_usage_na','stack_db_usage_na_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('az_stack_db_usage_na','stack_db_usage_na_f')}}) 
    )
    where rn = 1

    union all

    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        NULL as TERMS,
        NULL as BRANCHES,
        NULL as TAXONOMIES,
    	'AZ' as PLATFORM,
    	'EU' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('az_stack_db_usage_eu','stack_db_usage_eu_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('az_stack_db_usage_eu','stack_db_usage_eu_f')}}) 
    )
    where rn = 1

    union all

    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        TERMS,
        BRANCHES,
        TAXONOMIES,
    	'GCP' as PLATFORM,
    	'NA' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('gcp_stack_db_usage_na','stack_db_usage_na_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('gcp_stack_db_usage_na','stack_db_usage_na_f')}}) 
    )
    where rn = 1

    union all

    select
        SOURCE_FILE_NAME,
        FILE_ROW_NUMBER,
        BATCH_NUMBER,
        DATA_SYNC_DATE,
        ORG_UID,
        ORG_NAME,
        STACK_NAME,
        OWNER_EMAIL,
        STACK_API_KEY,
        CONTENT_TYPES,
        ENTRIES,
        ASSETS,
        ENVIRONMENTS,
        LOCALES,
        ROLES,
        WEBHOOKS,
        EXTENSIONS,
        SAVED_SEARCHES,
        CUSTOM_ROLES,
        GLOBAL_FIELDS,
        DATE,
        TERMS,
        BRANCHES,
        TAXONOMIES,
    	'GCP' as PLATFORM,
    	'EU' as LOCATION
    from
    (
        select 
        * ,
        row_number() over (partition by date, org_uid, org_name, stack_name, stack_api_key order by data_sync_date desc) as rn
        from {{source('gcp_stack_db_usage_eu','stack_db_usage_eu_f')}}
        where 1=1
            and data_sync_date = (select max(data_sync_date) from {{source('gcp_stack_db_usage_eu','stack_db_usage_eu_f')}}) 
    )
    where rn = 1
