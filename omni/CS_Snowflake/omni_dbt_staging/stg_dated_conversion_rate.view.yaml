# Reference this view as omni_dbt_staging__stg_dated_conversion_rate
schema_label: ""
#This description was pulled from dbt.
description: Staging model for dated currency conversion rates from Salesforce
  or Fivetran. Provides standardized currency conversion rates over time for use
  in financial and reporting models.

schema: omni_dbt_staging
table_name: STG_DATED_CONVERSION_RATE

dimensions:
  iso_code:
    sql: '"ISO_CODE"'
    #This description was pulled from dbt.
    description: ISO currency code (e.g., USD, EUR, INR) representing the base currency.

  conversion_rate:
    sql: '"CONVERSION_RATE"'
    #This description was pulled from dbt.
    description: Conversion rate from the base currency to the specified ISO
      currency on the given start date.

  start_date:
    sql: '"START_DATE"'
    #This description was pulled from dbt.
    description: Effective start date for the conversion rate.

  next_start_date:
    sql: '"NEXT_START_DATE"'
    #This description was pulled from dbt.
    description: Date when the next conversion rate becomes effective. Useful for
      determining the active conversion rate for any given date.

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_dated_conversion_rate
  target_schema: STAGING
  description: Staging model for dated currency conversion rates from Salesforce
    or Fivetran. Provides standardized currency conversion rates over time for
    use in financial and reporting models.
  config:
    schema: staging
    materialized: view
  code: |-
    -- models/staging/stg_dated_conversion_rate.sql
    select
        iso_code,
        conversion_rate,
        start_date,
        next_start_date
    from fivetran_database.salesforce.dated_conversion_rate
  referenced_by: [ fact_dreamguard_all, fact_dreamguard_main, fact_dreamguard_main_fiscal ]
