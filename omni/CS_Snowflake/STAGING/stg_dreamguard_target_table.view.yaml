# Reference this view as staging__stg_dreamguard_target_table
schema: STAGING
table_name: STG_DREAMGUARD_TARGET_TABLE

dimensions:
  source_file_name:
    sql: '"SOURCE_FILE_NAME"'

  file_row_number:
    sql: '"FILE_ROW_NUMBER"'

  batch_number:
    sql: '"BATCH_NUMBER"'

  data_sync_date:
    sql: '"DATA_SYNC_DATE"'

  metric_date:
    sql: TO_DATE(METRIC_DATE,'YYYY-DD-MM')
    label: Date_Metric Date
    group_label: Date_Metric Date

  quarter:
    sql: '"QUARTER"'

  fiscal_year:
    sql: '"FISCAL_YEAR"'

  metric_name:
    sql: '"METRIC_NAME"'

  sub_region_l1:
    sql: '"SUB_REGION_L1"'

  sub_region_l2:
    sql: '"SUB_REGION_L2"'

  type:
    sql: '"TYPE"'

  motion:
    sql: '"MOTION"'

  sub_motion:
    sql: '"SUB_MOTION"'

  company_size:
    sql: '"COMPANY_SIZE"'

  new_goal__:
    sql: '"NEW_GOAL_$"'

  product:
    sql: '"PRODUCT"'

  expansion_motion:
    sql: '"EXPANSION_MOTION"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_dreamguard_target_table
  target_schema: STAGING
  config:
    schema: staging
    materialized: view
  code: |-
    with source as (
        select * from {{ source('dreamguard_targets', 'dream_guard_targets') }} 
    )

    select
    *
    from source
