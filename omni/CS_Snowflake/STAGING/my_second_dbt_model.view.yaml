# Reference this view as staging__my_second_dbt_model
schema: STAGING
table_name: MY_SECOND_DBT_MODEL

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
