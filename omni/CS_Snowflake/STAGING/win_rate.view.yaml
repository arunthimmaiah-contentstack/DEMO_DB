# Reference this view as staging__win_rate
schema: STAGING
table_name: WIN_RATE

dimensions:
  opportunity_close_date:
    sql: '"OPPORTUNITY_CLOSE_DATE"'

  win_rate:
    sql: '"WIN_RATE"'

measures:
  count:
    aggregate_type: count
