# Reference this view as staging__int_dau
schema: STAGING
table_name: INT_DAU

dimensions:
  date:
    sql: '"DATE"'

  organization_uid:
    sql: '"ORGANIZATION_UID"'

  location:
    sql: '"LOCATION"'

  platform:
    sql: '"PLATFORM"'

  dau:
    sql: '"DAU"'

measures:
  count:
    aggregate_type: count
